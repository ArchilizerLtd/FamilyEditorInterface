<Window x:Class="FamilyEditorInterface.WPF.FamilyParameterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FamilyEditorInterface.WPF;assembly="
        mc:Ignorable="d"
        Title="FamilyParameterView" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Style.Setters>
                <Setter Property="local:InputBindingsManager.UpdatePropertySourceWhenEnterPressed" Value="TextBox.Text"/>
            </Style.Setters>
        </Style>
        <local:RevitToUnitConverter x:Key="ReverseUnitConverter"></local:RevitToUnitConverter>
        <local:MultiplyConverter x:Key="MultiplyConverter"></local:MultiplyConverter>
    </Window.Resources>
    <Grid Margin="20">
        <ItemsControl ItemsSource="{Binding ValueParameters}" Grid.IsSharedSizeScope="True">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!--The Name-->
                        <TextBox Text="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Width="200"/>
                        <!--The Value Slider-->
                        <Slider Value="{Binding Value, Delay=500}"
                                Maximum="{Binding Value, Mode=OneTime, Converter={StaticResource MultiplyConverter}}"  Grid.Column="1" Margin="5"/>
                        <!--The Value-->
                        <TextBox Text="{Binding Value, UpdateSourceTrigger=LostFocus, Converter={StaticResource ReverseUnitConverter} }" Grid.Column="2" Width="100"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
