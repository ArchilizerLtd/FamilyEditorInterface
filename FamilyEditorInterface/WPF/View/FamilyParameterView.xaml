<Controls:MetroWindow x:Class="FamilyEditorInterface.WPF.FamilyParameterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FamilyEditorInterface.WPF;assembly="                      
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        x:Name="MainWindow"
        GlowBrush="{DynamicResource AccentColorBrush}"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Title="Family Parameters" Height="600" MaxWidth="400" Width="400">
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
                <Style.Setters>
                    <Setter Property="local:InputBindingsManager.UpdatePropertySourceWhenEnterPressed" Value="TextBox.Text"/>
                </Style.Setters>
            </Style>
            <local:RevitToUnitConverter x:Key="ReverseUnitConverter"></local:RevitToUnitConverter>
            <local:MultiplyConverter x:Key="MultiplyConverter"></local:MultiplyConverter>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FamilyEditorInterface;component/Resources/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Margin="20,20,20,20">
        <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="0,25,0,0">
            <Button x:Name="btnShuffle" Style="{DynamicResource MetroCircleButtonStyle}" Height="30" Width="40" BorderThickness="0" Margin="0 0 60 0" Content="Shuffle"/>
            <Button IsDefault="True" Name="btnDialogOk" Click="btnDialogOk_Click" MinWidth="85" Margin="0,0,10,0">_Ok</Button>
            <Button IsCancel="True" MinWidth="85">_Cancel</Button>
        </WrapPanel>
        <ItemsControl ItemsSource="{Binding ValueParameters}" Grid.IsSharedSizeScope="True">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!--The Name-->
                        <TextBox Text="{Binding Path=Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Width="60" BorderThickness="0"/>
                        <!--The Value Slider-->
                        <Slider Value="{Binding Value, Delay=500}"
                                Maximum="{Binding Value, Mode=OneTime, Converter={StaticResource MultiplyConverter}}"  Grid.Column="1" Margin="5"/>
                        <!--The Value-->
                        <TextBox Text="{Binding Value, UpdateSourceTrigger=LostFocus, Converter={StaticResource ReverseUnitConverter} }" Grid.Column="2" Width="60" BorderThickness="0"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</Controls:MetroWindow>
